SQLの極値関数(MIN/MAX)は、いずれもソートを発生させる。
引数の列にインデックスが存在する場合、そのインデックスのスキャンだけで済ませ、
実表への検索を回避できる。

ユニークインデックスの場合一層効果がある。(ユニークインデックスは重複を許可しない)
複合インデックスの場合、先頭列であれば有効(複合インデックスは複数のカラムのペアでのインデックス)
複合インデックスの場合、ソート自体を無くしているわけではないが、
その前段の検索を高速化することによる軽減措置。